import React from 'react'

class Background extends React.Component {
    render () {
        return (
        <header>
        <h1>testing background image, trying to get it behind table too</h1>
        </header>
    )
    }
}

export default Background
